on: [pull_request]

jobs:
  build:
    name: Build Validation
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm ci
      - name: Run Build
        run: npm build
    
